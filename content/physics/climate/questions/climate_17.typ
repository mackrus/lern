#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)

Why are western boundary currents (e.g. Gulf Stream, Kuroshio) much faster and narrower than eastern boundary currents?
#pagebreak()
#set page(width: a_width, height: auto, margin: a_margin)
#set text(size: a_size)
Ocean basins are drastically deeper in the west
#pagebreak()
#set page(width: a_width, height: auto, margin: a_margin)
#set text(size: a_size)
The latitudinal gradient of the Coriolis parameter ($beta = (partial f) / (partial y) > 0$) causes western intensification of wind-driven gyres
#pagebreak()
#set page(width: a_width, height: auto, margin: a_margin)
#set text(size: a_size)
Rapid continental drift in the west pulls the water column
#pagebreak()
#set page(width: a_width, height: auto, margin: a_margin)
#set text(size: a_size)
Equatorial thermal expansion drives flow only along western boundaries
#expl_page(include "../explanations/climate_17.typ")
#formulae_page(include "../formulae/climate_core.typ")
#steps_page(include "../solution_steps/climate_17.typ")
