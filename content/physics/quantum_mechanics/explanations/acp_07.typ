#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

The electron-Volt ($e V$) directly quantifies the energy an electron gains accelerating across a 1-volt electric potential.
