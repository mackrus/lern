#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

By the Landé interval rule, the energy separation between adjacent $J$ levels is $Delta E = lambda J$. Isolating $lambda$ gives $lambda = Delta E / J$.
