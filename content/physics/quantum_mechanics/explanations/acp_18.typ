#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

*(Applying correction)* To observe a deterministic outcome for operator $A$ without breaking entanglement, the spectrum of $A$ must be degenerate to preserve the entanglement within that specific subspace.
