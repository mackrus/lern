#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

A matrix both Unitary ($U^dagger U = I$) and Hermitian ($U = U^dagger$) strictly satisfies $U^2 = I$. Its eigenvalues must square to 1, leaving only $plus.minus 1$.
