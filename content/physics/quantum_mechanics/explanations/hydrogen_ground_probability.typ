#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Compute the overlap integral $chevron.l psi_(100) | psi chevron.r = 4 pi integral psi_(100) psi r^2 d r$. Substituting the specific constants and utilizing the provided Gamma function identity yields an overlap $I$, which, when squared for probability, results in $75 pi / 256$.
