#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Maximize $S$ ($S=1$), maximize $L$ ($L=3$, defining an $F$ state), and minimize $J$ for a less-than-half-full shell ($J = |L-S| = 2$). The resulting term is $""^3F_2$.
