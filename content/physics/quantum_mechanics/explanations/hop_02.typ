#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

$H'$ contains operators that shift the state by $plus.minus 6$ or leave it unchanged. The diagonal term drops out of the second-order sum because $m != n$, leaving only states $|n+6chevron.r$ and $|n-6chevron.r$.
