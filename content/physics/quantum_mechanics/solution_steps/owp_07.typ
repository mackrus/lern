#set text(size: 20pt)
*Solution Steps Walkthrough:*

1. Write the normalization condition for the wavefunction: $integral_(-infinity)^(infinity) |psi(x)|^2 d x = 1$ (or $sum |c_n|^2 = 1$ for discrete states).

2. Solve the integral or sum for the normalization constant $A$.

3. Write the expectation value of an operator $Q$ as $chevron.l Q chevron.r = integral psi^* Q psi d x$ (or $chevron.l Q chevron.r = sum p_n q_n$ for discrete outcomes).

4. Perform the integration or summation using symmetry properties of the integrand (such as even/odd parity) to simplify the calculation.
