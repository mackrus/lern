#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)
Let $A, B$ be two Hermitian operators such that $[A,B] != 0$. Then:
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
There exists no state that is deterministic for both
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
We cannot diagonalize $A$ and $B$ simultaneously
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$sigma_A sigma_B > 0$
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
All of the above
#expl_page(include "../explanations/acp_14.typ")
#prereq_page(include "../prerequisites/conceptual_practice.typ")
