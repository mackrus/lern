#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)
If the spectrum is continuous, we normalize basis states to the Dirac delta function; if the spectrum is discrete, we normalize to the Kronecker delta.
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
True
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
False
#pagebreak()
#include "../explanations/acp_15.typ"
#pagebreak()
#include "../prerequisites/advanced_conceptual.typ"
