#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)
The spin of a particle determines the nature of the transformation of the state vector under spatial rotations; the precise details also depend on the projection of the spin.
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
True
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
False
#expl_page(include "../explanations/acp_16.typ")
#prereq_page([
#include "../prerequisites/core/sam_spin-1_2_operators.typ"
])
