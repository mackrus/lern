#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)
You measure the energy of a particle in an infinite square well, then immediately measure its position, and then immediately measure its energy again. What are the possible outcomes of the second energy measurement?
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Any energy eigenvalue $E_n$ is possible
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Only the original energy value is possible
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Only $E_1, E_2, E_3$ are possible
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Only the ground state is possible
#pagebreak()
#include "../explanations/wms_01.typ"
#pagebreak()
#include "../prerequisites/conceptual_foundations.typ"
