#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
*Dimensionality:* The normalization condition requires the total probability integral over all $d$-dimensional space to be dimensionless (equal to 1). This constraints the physical dimensions of the wavefunction amplitude in terms of length $L$.
