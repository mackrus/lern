#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
*Spectrum Normalization:* Orthonormality conditions differ depending on whether the eigenvalues of an operator form a discrete set or a continuous range, requiring different mathematical delta distributions to represent orthonormality.
