#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
*WKB Approximation:* Assumes the de Broglie wavelength changes slowly ($|d lambda / d x| << 1$). It fails at classical turning points where $p -> 0$ and $lambda -> infinity$.
