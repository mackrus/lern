#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

- *(a)*: Use the First Law in a control volume (throttling valve) to show that $Delta H = 0$.
- *(b)*: Use the cyclic identity $((partial T) / (partial P))_H = -((partial H) / (partial P))_T / ((partial H) / (partial T))_P$ and Maxwell relations to express the derivatives in terms of known properties ($C_p, beta, V, T$).
