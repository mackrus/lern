#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

- *Isothermal*: $Delta U = 0$, $Q = W_("rev") = n R T ln(V_("final") / V_("initial"))$.
- *Adiabatic*: $Q = 0$, $Delta U = n c_v Delta T$, $W = Delta U$.
Follow the temperature changes and volume ratios for each step.
