TYPST SYNTAX ERROR ANALYSIS REPORT

Below is a detailed breakdown of all Typst syntax errors, Markdown-isms, and LaTeX-isms found across the analyzed .typ files.

================================================================================
1. PREREQUISITE FILES (thermodynamics/prerequisites/)
================================================================================

File: ideal_gas_states.typ
  - Line 1: Markdown heading syntax `# Ideal Gas Basics & State Transitions` instead of Typst Heading (=). 
    In Typst, `#` initiates a code block/expression, causing parsing failures.
  - Lines 5, 11: Markdown heading syntax `## Key Processes` and `## State Coordinates` instead of Typst subheadings (==).

File: entropy_second_law.typ
  - Line 1: Markdown heading syntax `# Entropy and the Second Law` instead of Typst Heading (=).
  - Line 5: Markdown heading syntax `## Key Concepts` instead of Typst subheading (==).
  - Line 4: LaTeX command `\ge` used in `$dS \ge 0$`. In Typst math mode, "greater than or equal to" is written as `>=`.
  - Line 6: LaTeX subscript grouping braces `_{rev}` used in `$dS = dQ_{rev} / T$`. Typst uses `_rev` or `_("rev")`; curly braces `{}` are reserved for code blocks.
  - Line 7: LaTeX commands `\ln` used twice in `$dS = n c_v \ln(T_2/T_1) + n R \ln(V_2/V_1)$`. In Typst, standard math functions do not take a backslash, so this should be `ln`.

File: thermodynamic_potentials.typ
  - Line 1: Markdown heading syntax `# Thermodynamic Potentials and Maxwell Relations` instead of `=`.
  - Lines 5, 12: Markdown heading syntax `## Key Potentials` and `## Maxwell Relations` instead of `==`.
  - Lines 14-17: LaTeX command `\partial` used multiple times throughout the Maxwell relation equations. Typst math mode uses the un-backslashed literal `partial`.

File: heat_engines.typ
  - Line 1: Markdown heading syntax `# Heat Engines and Cycles` instead of `=`.
  - Line 5: Markdown heading syntax `## Important Cycles` instead of `==`.
  - Line 10: LaTeX command `\oint` used in `$\oint dU = 0$` and `$\oint dQ = \oint dW$`. In Typst, this is written as `oint` without a backslash.

================================================================================
2. EXPLANATION FILES (thermodynamics/explanations/)
================================================================================

File: td_2025_06_u1.typ
  - Status: Clean. No syntax errors found.

File: td_2025_06_u2.typ
  - Lines 4-5: Multiline broken inline math expression. The line ends with `performing $|W|` and continues on the next line with `= 832$ J of work.`. 
    In Typst, inline math delimited by a single `$` cannot contain newlines. This leaves the first math statement unclosed and causes the second line to be treated as a level-1 heading because it starts with `=`.

File: td_2025_06_u3.typ
  - Line 8: Invalid subscript syntax `$d Q_(_rev)= 0$`. The nested leading underscore inside the parenthesis `_(_rev)` is invalid. It should be written as `_rev` or `_("rev")`.
  - Line 9: Invalid subscript syntax and concatenated identifier `$d S = n c_(_vln)(T_2/T_1)$`. Similar to above, `_(_vln)` contains an invalid leading underscore inside parentheses, and the natural log function `ln` is incorrectly baked into the subscript string name.

File: td_2025_06_u4.typ
  - Line 6: Invalid subscript syntax and concatenated identifier `$d S = n c_(_pln)(T_2/T_1)$`. Contains the exact same malformed subscript structure as U3 above.

File: td_2025_06_u5.typ, td_2025_06_u6.typ, td_2025_06_u7.typ, td_2025_06_u8.typ
  - Status: Clean. No syntax errors found.

File: td_2025_08_u1.typ
  - Line 9: Invalid subscript syntax `$W_(_net)$`. Contains an incorrect leading underscore inside the parenthesis. Should be `W_net` or `W_("net")`.

File: td_2025_08_u2.typ
  - Line 8: Multiple invalid subscript expressions: `$W_(_rev)`, `V_(_final)`, and `V_(_initial)`. All possess an incorrect leading underscore within the subscript grouping parentheses.

File: td_2025_08_u3.typ
  - Line 6: Invalid subscript syntax `$W_(_by)$`. Contains an incorrect leading underscore inside the grouping parenthesis.

File: td_2025_08_u4.typ
  - Line 6: LaTeX commands `\int` and `\frac` used in `$d S = \int n c \frac{d T}{T}$`. In Typst math mode, integrals are written as `integral` or `int`, and fractions use standard division notation `/`.

File: td_2025_08_u5.typ
  - Status: Clean. No syntax errors found.

File: td_2025_08_u6.typ
  - Line 5: LaTeX commands `\sin` and `\theta` used in `$M g v \sin(\theta)$`. Typst requires un-backslashed identifiers `sin` and `theta`.

File: td_2025_08_u7.typ
  - Line 5: Heavy LaTeX syntax used in `$\mu_{JT} = \frac{V}{C_p}(beta T - 1)$`. Commands like `\mu` and `\frac`, along with curly brace subscript groupings `{JT}`, are invalid in Typst. This should be written as `mu_("JT") = V / C_p (beta T - 1)`.

File: td_2025_08_u8.typ
  - Line 5: LaTeX command `\cdot` used in `$(partial T / partial P)_S\cdot (partial P / partial S)_T\cdot (partial S / partial T)_P= -1$`. In Typst, a centered dot is represented by the `cdot` keyword or a simple space/dot operator.