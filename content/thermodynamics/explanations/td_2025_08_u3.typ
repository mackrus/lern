#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

- *Entropy*: $d S = n R ln(V_2/V_1)$. Use this to find the volume ratio.
- *Work*: $W_("by") = n R T ln(V_2/V_1)$. With $W$ and the volume ratio known, calculate $T$.
