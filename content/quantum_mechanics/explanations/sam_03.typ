#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Antiferromagnetic coupling ($beta, gamma > 0$) means the system minimizes energy when spins are anti-aligned. The sum of the paired spins drops to zero, yielding a ground state of $F=0$.
