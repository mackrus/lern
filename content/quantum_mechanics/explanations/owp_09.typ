#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Wave packet spreading is governed by the timescale $tau = 2m sigma_x^2 / planck$. The quantum dispersion visually dominates over the classical path trajectory when $t \gg tau$.
