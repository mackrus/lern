#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

The eigenvalues of $vec(n) dot vec(sigma)$ are strictly $plus.minus 1$ for any normal vector. Scaling the Hamiltonian by $gamma$ makes the eigenvalues $plus.minus gamma$.
