#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Measuring position collapses the state into a position eigenstate (a delta function), which mathematically constitutes an infinite superposition of all possible energy eigenstates. Therefore, all $E_n$ are possible subsequent outcomes.
