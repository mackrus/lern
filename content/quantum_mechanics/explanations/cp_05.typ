#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Two operators commuting on a single, specific shared eigenvector does not mean $[A,B]=0$ holds across the entire Hilbert space.
