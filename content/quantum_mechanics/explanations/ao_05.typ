#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Because the azimuthal angle $phi$ wraps around the origin, physically meaningful wavefunctions must be single-valued: $Y(phi) = Y(phi + 2 pi)$.
