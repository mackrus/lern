#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Integrate $|Psi(x)|^2$ from $-L$ to $L$ and set to 1. By symmetry, solving $2 A^2 integral_0^L (1 - x/L)^2 d x = 1$ yields $A = sqrt(3 / (2L))$.
