#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

To prevent the wave function from diverging as $e^(2kappa r)$ and remain normalizable at infinity, the polynomial expansion $P(r)$ must forcefully terminate.
