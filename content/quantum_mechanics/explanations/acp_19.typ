#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

The Variational Principle guarantees that substituting $E_0$ for all $E_n$ when expanding a trial state produces a strict lower bound: $chevron.l H chevron.r >= E_0$.
