#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

In 3D, integration over volume ($L^3$) must equal a dimensionless 1. Consequently, the wavefunction amplitude must carry units of $L^(-3/2)$.
