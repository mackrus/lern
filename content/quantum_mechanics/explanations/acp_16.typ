#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Spin strictly defines how a spinor transforms under "SU(2)" rotations; the exact phase mixing intrinsically depends on the particle's spin projection.
