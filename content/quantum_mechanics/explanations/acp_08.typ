#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

If the probability density current $j=0$ everywhere, there is zero net flow of probability, which mathematically forces the expectation value of momentum $chevron.l p chevron.r$ to zero.
