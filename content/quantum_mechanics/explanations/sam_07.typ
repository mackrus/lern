#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

An $F_x$ perturbation drives magnetic dipole transitions. The standard selection rule for the total angular momentum projection is $Delta M = plus.minus 1$.
