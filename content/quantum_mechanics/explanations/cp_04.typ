#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

The probability density $|Psi|^2$ is time-independent, but the full stationary solution $Psi(x,t) = psi(x)e^(-i E t/planck)$ natively oscillates over time.
