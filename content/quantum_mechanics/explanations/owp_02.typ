#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

The ground state condition $a Psi_0 = 0$ yields a first-order differential equation. Normalizing its Gaussian solution provides the coefficient $A = (m omega/pi planck)^(1/4)$.
