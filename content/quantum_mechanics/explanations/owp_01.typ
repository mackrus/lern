#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

This is a standard commutator identity resulting from repeated application of $[a, a^dagger] = 1$, yielding $[a, (a^dagger)^n] = n(a^dagger)^(n-1)$.
