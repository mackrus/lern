#import "../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)
A beam of spin-1/2 particles is passed through a sequence of Stern-Gerlach (SG) filters. Compute the eigenvalues of the Hamiltonian $H = gamma (n dot sigma)$ representing the interaction with the SG magnetic field.
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$plus.minus gamma$
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$plus.minus 1$
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$0, gamma$
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$gamma$
#pagebreak()
#include "../explanations/sam_01.typ"
#pagebreak()
#include "../prerequisites/spin_angular_momentum.typ"
