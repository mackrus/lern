#import "../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)
You measure the energy of a particle in an infinite square well, then immediately measure its position, and then immediately measure its energy again. If the first energy measurement gave $E_1$, what is the probability of measuring $E_1$ again in the second energy measurement?
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
It depends on where the position measurement result was found
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
1
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
0
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$1/2$
#pagebreak()
#include "../explanations/wms_02.typ"
#pagebreak()
#include "../prerequisites/conceptual_foundations.typ"
