#import "../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)
#set enum(numbering: "a)", ..list_style)
In the double slit experiment, particles are detected one at a time. What pattern eventually appears on the screen?
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Two distinct bands
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
A uniform distribution
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
An interference pattern
#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
A single central peak
#pagebreak()
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Even for single particles, the probability distribution follows an interference pattern due to the superposition of paths.
#pagebreak()
#include "../prerequisites/conceptual_foundations.typ"
