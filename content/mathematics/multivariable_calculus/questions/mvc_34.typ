#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Let $E$ be the region in $RR^3$ given by $x^2 + y^2 + 9 z^2 <= 9, z >= 0$. Compute the triple integral of $(x + y + z) d x d y d z$.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 9 pi / 4 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 9 pi / 2 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 9 pi / 8 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 18 pi $

#expl_page(include "../explanations/mvc_34.typ")
#formulae_page(include "../formulae/mvc_34.typ")
#steps_page(include "../solution_steps/mvc_34.typ")
#prereq_page(include "../prerequisites/mvc_34.typ")
