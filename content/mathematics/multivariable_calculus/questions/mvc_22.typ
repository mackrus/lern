#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Compute the triple integral
$ integral.triple_E x d V $
where $E$ is the domain in $RR^3$ bounded by the surfaces $x = 0$, $y = 0$, $z = x + y$, and $z = 2 - x - y$.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 / 12 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 / 6 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 / 24 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 / 8 $

#expl_page(include "../explanations/mvc_22.typ")
#formulae_page(include "../formulae/mvc_22.typ")
#steps_page(include "../solution_steps/mvc_22.typ")
#prereq_page(include "../prerequisites/mvc_22.typ")
