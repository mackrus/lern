#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Find the minimum distance from the origin $(0, 0, 0)$ to the curve of intersection formed by the ellipsoid $x^2 + 2 y^2 + z^2 = 10$ and the plane $x + y + z = 1$.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ sqrt(153 - 8 sqrt(6)) / 5 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ sqrt(153 - 8 sqrt(6)) / 25 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ sqrt(5) $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 $

#expl_page(include "../explanations/mvc_05.typ")
#formulae_page(include "../formulae/mvc_05.typ")
#steps_page(include "../solution_steps/mvc_05.typ")
#prereq_page(include "../prerequisites/mvc_05.typ")
