#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Let $D$ be the region in $RR^2$ bounded by $y = x - 1$ and $y^2 + 2 y - x + 1 = 0$. Compute the area of $D$.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 / 6 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 / 3 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 5 / 6 $#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 1 / 12 $

#expl_page(include "../explanations/mvc_33.typ")
#formulae_page(include "../formulae/mvc_33.typ")
#steps_page(include "../solution_steps/mvc_33.typ")
#prereq_page(include "../prerequisites/mvc_33.typ")
