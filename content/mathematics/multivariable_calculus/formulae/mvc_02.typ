#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
*Key Formula:*
- *Directional Derivative:* $D_(bold(u)) f(P) = nabla f(P) dot bold(u)$ where $bold(u) = bold(v) / ||bold(v)||$ is the unit vector.
