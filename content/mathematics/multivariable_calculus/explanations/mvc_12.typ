#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

We check that the column vectors of $A$ form an orthonormal set. Since $A$ is orthogonal, $A^{-1} = A^T$. Furthermore, $A$ is symmetric ($A^T = A$), so $A^{-1} = A$.
