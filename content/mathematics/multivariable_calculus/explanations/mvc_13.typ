#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Since $A^{-1} = A$, we have $A^2 = I$. Therefore, any even power of $A$ is the identity matrix $I$, and any odd power of $A$ is $A$ itself. Since 2025 is odd, $A^{2025} = A$.
