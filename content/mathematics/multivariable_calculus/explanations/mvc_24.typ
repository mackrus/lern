#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Since $T$ is a projection, applying it twice has the same effect as applying it once. Thus, the composition matrix is $[T]^2 = [T]$.
