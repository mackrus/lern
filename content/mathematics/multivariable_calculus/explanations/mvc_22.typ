#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

The region $E$ projects onto the $x y$-plane as the triangle bounded by $x=0, y=0$, and $x+y=1$. The limits are $0 <= x <= 1$, $0 <= y <= 1-x$, and $x+y <= z <= 2-x-y$. Computing the iterated integral yields $1/12$.
