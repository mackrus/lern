#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

Using row operations to introduce zeros, we can expand along rows to reduce the size of the matrix, eventually yielding the factored form of the determinant: $x(x-1)(x-2)(x+1)$.
