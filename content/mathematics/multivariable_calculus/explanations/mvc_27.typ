#import "/content/template.typ": *
#set page(width: i_width, height: auto, margin: i_margin)
#set text(size: i_size)
Explanation:

We rewrite the function as $y dot (sin(x^2+y^2))/(x^2+y^2)$. Since $lim_(t -> 0) (sin(t))/t = 1$ and the term $y$ goes to $0$, the overall limit is $0 dot 1 = 0$.
