#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Identify the set of points in the complex plane satisfying the inequality:
$ |z - 1|^2 + |z + 1|^2 < 8 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
An open disk $|z| < sqrt(3)$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
An open annulus $1 < |z| < 2$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
The interior of an ellipse with foci at $plus.minus 1$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
The empty set

#expl_page(include "../explanations/ca_25.typ")
#formulae_page(include "../formulae/ca_25.typ")
#steps_page(include "../solution_steps/ca_25.typ")
#prereq_page(include "../prerequisites/ca_25.typ")
