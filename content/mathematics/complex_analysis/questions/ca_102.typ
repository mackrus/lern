#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

In which quadrants of the complex plane do the four roots of $p(z) = z^4 + z^3 + 4 z^2 + 2 z + 3 = 0$ lie?

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Two roots in Quadrant II and two roots in Quadrant III

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
One root in each of the four quadrants

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Two roots in Quadrant I and two roots in Quadrant IV

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
All four roots on the imaginary axis

#expl_page(include "../explanations/ca_102.typ")
#formulae_page(include "../formulae/ca_102.typ")
#steps_page(include "../solution_steps/ca_102.typ")
#prereq_page(include "../prerequisites/ca_102.typ")
