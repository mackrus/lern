#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Find all distinct values of the complex cube root $(-8)^(1/3)$ in cartesian form.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$-2$ and $1 plus.minus i sqrt(3)$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$2$ and $-1 plus.minus i sqrt(3)$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$-2 i$ and $plus.minus sqrt(3) + i$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$-2$ and $plus.minus 2 i$

#expl_page(include "../explanations/ca_27.typ")
#formulae_page(include "../formulae/ca_27.typ")
#steps_page(include "../solution_steps/ca_27.typ")
#prereq_page(include "../prerequisites/ca_27.typ")
