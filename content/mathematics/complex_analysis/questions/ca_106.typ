#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

For the polynomial $p(z) = z^7 + 3 z^5 - 6 z^2 + 1$, determine the number of zeros (counting multiplicity) in the regions $|z| < 1$, the annulus $1 < |z| < 2$, and the right half-plane ${"Re"(z) > 0}$, respectively.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$2, quad 5, quad 4$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$2, quad 4, quad 3$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$3, quad 4, quad 4$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$1, quad 6, quad 3$

#expl_page(include "../explanations/ca_106.typ")
#formulae_page(include "../formulae/ca_106.typ")
#steps_page(include "../solution_steps/ca_106.typ")
#prereq_page(include "../prerequisites/ca_106.typ")
