#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Find the principal argument $"Arg"(z)$ for the complex number $z = (-2) / (1 + sqrt(3) i)$.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ (2 pi) / 3 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ - (pi) / 3 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ (5 pi) / 6 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ - (2 pi) / 3 $

#expl_page(include "../explanations/ca_03.typ")
#formulae_page(include "../formulae/ca_03.typ")
#steps_page(include "../solution_steps/ca_03.typ")
#prereq_page(include "../prerequisites/ca_03.typ")
