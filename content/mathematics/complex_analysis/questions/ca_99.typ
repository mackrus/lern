#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

All zeros of the polynomial $p(z) = z^4 - 2 i z^3 + 16$ lie in the disk $|z| < 3$. How many of these zeros lie strictly in the third quadrant (${"Re"(z) < 0, "Im"(z) < 0}$)?

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$1$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$0$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$2$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$3$

#expl_page(include "../explanations/ca_99.typ")
#formulae_page(include "../formulae/ca_99.typ")
#steps_page(include "../solution_steps/ca_99.typ")
#prereq_page(include "../prerequisites/ca_99.typ")
