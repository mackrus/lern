#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Determine the set of all $x in RR$ on which the series of functions $sum_(n=1)^oo x^n / (n^3 + x^(2n))$ converges uniformly.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
All of $RR$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Only the closed interval $[-1, 1]$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Only the open interval $(-1, 1)$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
Only compact subsets of $RR without {plus.minus 1}$

#expl_page(include "../explanations/ca_74.typ")
#formulae_page(include "../formulae/ca_74.typ")
#steps_page(include "../solution_steps/ca_74.typ")
#prereq_page(include "../prerequisites/ca_74.typ")
