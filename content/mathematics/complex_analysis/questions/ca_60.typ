#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Compute the integral with respect to arc length:
$ integral_gamma |z - 1| thin |d z| $
where $gamma$ is the positively oriented unit circle $|z| = 1$.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$8$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$4 pi$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$4$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$2 pi$

#expl_page(include "../explanations/ca_60.typ")
#formulae_page(include "../formulae/ca_60.typ")
#steps_page(include "../solution_steps/ca_60.typ")
#prereq_page(include "../prerequisites/ca_60.typ")
