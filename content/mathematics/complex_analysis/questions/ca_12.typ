#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Let $C$ denote the positively oriented boundary of the square with sides along $x = plus.minus 2$ and $y = plus.minus 2$. Evaluate the contour integral:
$ integral_C (cosh z) / z^4 thin d z $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 0 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ (pi i) / 3 $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ 2 pi i $

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$ (2 pi i) / 3 $

#expl_page(include "../explanations/ca_12.typ")
#formulae_page(include "../formulae/ca_12.typ")
#steps_page(include "../solution_steps/ca_12.typ")
#prereq_page(include "../prerequisites/ca_12.typ")
