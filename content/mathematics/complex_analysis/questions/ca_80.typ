#import "../../../template.typ": *
#set page(width: q_width, height: auto, margin: q_margin)
#set text(size: q_size)
#set list(..list_style)

Determine the radius of convergence of the power series expansion of $f(z) = (z^2 - 1) / (z^3 - 1)$ centered at $z = 2$.

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$sqrt(7)$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$1$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$3$

#pagebreak()
#set page(width: a_width, margin: a_margin)
#set text(size: a_size)
$sqrt(3)$

#expl_page(include "../explanations/ca_80.typ")
#formulae_page(include "../formulae/ca_80.typ")
#steps_page(include "../solution_steps/ca_80.typ")
#prereq_page(include "../prerequisites/ca_80.typ")
